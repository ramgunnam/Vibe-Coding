# Salesforce Deployment Configuration
# Copy this file to .env and customize for your environment
# Usage: source .env && ./scripts/deploy-to-sandbox.sh

# =============================================================================
# Sandbox Configuration
# =============================================================================

# Salesforce sandbox alias (as configured in sf CLI)
SF_SANDBOX_ALIAS=dodd-sandbox

# Salesforce instance URL for sandbox authentication
# Use https://test.salesforce.com for sandboxes
# Use https://login.salesforce.com for production (not recommended for this script)
SF_INSTANCE_URL=https://test.salesforce.com

# =============================================================================
# Deployment Configuration
# =============================================================================

# Source directory containing Salesforce metadata
SF_SOURCE_DIR=force-app/main/default

# Salesforce API version
SF_API_VERSION=66.0

# Test level for deployment
# Options:
#   NoTestRun        - No tests executed (use carefully, may be blocked by org policies)
#   RunLocalTests    - Run tests in your org except from managed packages (default)
#   RunAllTestsInOrg - Run all tests including managed packages
SF_TEST_LEVEL=RunLocalTests

# =============================================================================
# Git Configuration (optional)
# =============================================================================

# Branch to deploy from (for CI/CD pipelines)
GIT_DEPLOY_BRANCH=main

# GitHub repository URL
GITHUB_REPO=https://github.com/ramgunnam/Vibe-Coding.git
